{
  "name": "pendencias-professor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch database.json --port 3001",
    "start": "concurrently \"npm run dev\" \"npm run server\""
  },
  "devDependencies": {
    "vite": "^8.2.2"
  },
  "dependencies": {
    "concurrently": "^10.0.5",
    "json-server": "^1.0.0-beta.15"
  }
}
