# Criar tabela de pendências

**Objetivo:** Criar um script SQL independente para a tabela `pendencias` com os campos e status solicitados.

**Tech Stack:** PostgreSQL / Supabase

---

## Status Geral

| Passo | Descrição | Status |
|-------|-----------|--------|
| 1 | Definir a estrutura da tabela | ✅ Concluído |
| 2 | Criar o script SQL | ✅ Concluído |
| 3 | Commit e push | ✅ Concluído |

---

### Passo 1: Definir a estrutura

**Status:** ✅ Concluído

**Arquivo:** `C:\fontes\professor-senai\sistema\TABELA_PENDENCIAS.sql`

**Ação:** Usar chave primária auto-incremental, descrição obrigatória, observação textual opcional, data atual como padrão e restrição para os quatro status informados.

**Verificação:** Inspeção estática do SQL, sem executar testes conforme as regras do projeto.

---

### Passo 2: Criar o script SQL

**Status:** ✅ Concluído

**Arquivo:** `C:\fontes\professor-senai\sistema\TABELA_PENDENCIAS.sql`

**Código:**

```sql
create table if not exists public.pendencias (
  id bigint generated by default as identity primary key,
  descricao text not null,
  obs text,
  status text not null default 'ABERTO'
    check (status in ('ANDAMENTO', 'ABERTO', 'CONCLUIDO', 'CANCELADO')),
  data date not null default current_date
);
```

**Verificação:** Inspeção estática do arquivo criado.

---

### Passo 3: Commit e push

**Status:** ✅ Concluído

**Ação:** Versionar somente o novo script e esta documentação.

**Verificação:** Confirmar a saída do Git após o envio.
